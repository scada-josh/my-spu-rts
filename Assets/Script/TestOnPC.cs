using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestOnPC : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // สร้างจาก เครื่อง PC กปน.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
